# react-native-language-module

## step 1. clone the repository


## step.2 run command yarn install

## step.3 run command npx react-native run-android  and if you have already built then npx react-native start
